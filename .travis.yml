language: python

install:
- sudo aptitude -y install libffi-dev btrfs-tools
- pip install --use-mirrors https://bitbucket.org/cffi/cffi/get/default.tar.gz
- pip install --use-mirrors tox
- git submodule update --init

script: tox

